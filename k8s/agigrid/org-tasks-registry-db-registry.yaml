apiVersion: apps/v1
kind: Deployment
metadata:
  name: org-tasks-registry-db-registry
  namespace: registries
spec:
  replicas: 1
  selector:
    matchLabels:
      app: org-tasks-registry-db-registry
  template:
    metadata:
      labels:
        app: org-tasks-registry-db-registry
    spec:
      containers:
        - name: org-tasks-registry-db-registry
          image: aiosv1/org-tasks-registry-db-registry:v1
          ports:
            - containerPort: 4000
          env:
            - name: MONGO_URL
              value: "mongodb://registry-0.registries.svc.cluster.local:27017"
---
apiVersion: v1
kind: Service
metadata:
  name: org-tasks-registry-db-registry-svc
  namespace: registries
spec:
  selector:
    app: org-tasks-registry-db-registry
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
      nodePort: 30216
  type: NodePort
